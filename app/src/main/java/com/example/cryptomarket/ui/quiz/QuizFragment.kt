package com.example.cryptomarket.ui.quiz

import android.os.Bundle
import android.view.View
import com.example.cryptomarket.BaseFragment
import com.example.cryptomarket.databinding.FragmentQuizBinding

class QuizFragment : BaseFragment<FragmentQuizBinding>(FragmentQuizBinding::inflate) {
    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        initViews()
        initListeners()
    }

    private fun initViews() {
        binding?.run {

        }
    }

    private fun initListeners() {
        binding?.run {

        }
    }
}